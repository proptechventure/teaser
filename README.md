# PDF Presentation Summarizer

## Installation
1. Install requirements: `pip install -r requirements.txt`
2. Run: `python app.py`
3. Open http://localhost:5000

## Features
- Extracts key slides (first, middle, last)
- Generates concise PDF summary
- Simple web interface